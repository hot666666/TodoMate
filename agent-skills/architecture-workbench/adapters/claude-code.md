# Claude Code adapter

Load `agent-skills/architecture-workbench/SKILL.md` as the task procedure after reading repository instructions. Maintain canonical Markdown from inspected source, then run the bundled validator and builder. Treat generated JSON and HTML as disposable output, never as the architecture source.
