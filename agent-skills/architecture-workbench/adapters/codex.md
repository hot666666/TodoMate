# Codex adapter

Invoke `$architecture-workbench`, read the repository's `AGENTS.md`, then follow the Skill workflow. Use local source and build metadata for facts. Run the bundled validator and builder before reporting completion. Never infer architecture from folder names alone.
